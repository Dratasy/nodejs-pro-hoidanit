

const handleCreateUser = (
    fullName: string,
    email: string,
    address: string) => {
        //insert into database 

        //return result

        console.log(`create user: ${fullName} - ${email} - ${address}`);
}

export {
    handleCreateUser
};